<div class="row">
  <h1><%= @gallery.name %></h1>

  <input type="hidden" id="galleryID" value="<%= @gallery.id %>">

  <p>
    <b>Description: </b>
    <%= @gallery.description %>
  </p>

  <div class="form-actions">
    <%= link_to '<< Back to Tour', tour_path(@gallery.tour), :class => 'btn btn-mini'  %>
    <%= link_to 'Edit gallery', edit_tour_gallery_path(@tour), :class => 'btn btn-mini' %>
    <%= link_to 'Delete gallery', tour_gallery_path(@tour), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-mini' %>
  </div>

<h2>Pictures</h2>
</div>

<div class="row">
  <ul class="thumbnails files">
<% unless @pictures.empty? %>
  <% @pictures.each do |pic| %>
    <li class="span3" id="picture_<%= pic.id %>">
    <div class="thumbnail">
      <%= image_tag pic.image.url %>
      <div class="caption">
        <p style="text-align: center;">
        <%= link_to "Edit picture", picture_path(pic) %>

        <%=
          link_to "Delete picture", pic,
            :data => { :confirm => "Are you sure?" },
            :method => :delete
          %>

        </p>
      </div>
    </div>
    </li>
  <% end %>
<% end %>
</ul>
</div>


